#:kivy 1.11.1
<Environment>:


<Cell>:
	canvas:
		Rectangle:
			#size and position on graph is different from those in program
			pos: self.pos_on_graph#: (self.x, self.y)
            size: self.size_on_graph#: (self.width, self.height)

	#GridLayout:
	#	cols: 10
	#	rows: 10